class Solution:
	def strStr(self, haystack: str, needle: str):
        if needle == '':
            return(0)
        else:
            if needle not in haystack:
                return(-1)
            else:
                helplist= haystack.split(needle)
                return(len(helplist[0]))
